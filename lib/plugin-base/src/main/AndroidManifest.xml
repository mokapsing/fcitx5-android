<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <!--
        ${mainApplicationId} can only be used inside plugin,
        because it's defined in AndroidPluginAppConventionPlugin
    -->
    <queries>
        <package android:name="com.iflytek.inputmethod.miui" />
    </queries>

    <application>
        <activity
            android:name=".AboutActivity"
            android:exported="true"
            android:theme="@style/DeviceSettingsTheme">
            <intent-filter>
                <action android:name="com.iflytek.inputmethod.miui.plugin.MANIFEST" />
            </intent-filter>
        </activity>
    </application>
</manifest>